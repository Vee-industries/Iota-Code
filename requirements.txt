# IOTA framework dependencies.
# Pinned to versions known to work with v0.82.0.30 (2026-05).
# For automated install, use: python setup.py
# Manual install: pip install -r requirements.txt
#                 (torch needs a CUDA-aware install first: see setup.py / README)

torch>=2.1.0
transformers>=4.40.0
accelerate>=0.27.0
bitsandbytes>=0.43.0
huggingface_hub>=0.20.0
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
matplotlib>=3.7.0
seaborn>=0.13.0
nvidia-ml-py>=12.0.0
flask>=3.0.0
jsonschema>=4.20.0
